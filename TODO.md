# TODO Status

- [x] Reason about backend ingest performance, implement a benchmark candidate, benchmark both, and record results in `perf_test.md`.
  - Result: the native HTTP candidate is faster in a synthetic in-memory benchmark, but the maintained Fastify service stays as the production implementation because it has the complete tested feature surface. The benchmark did uncover and fix a real code smell: default per-request info logging.
- [x] Add a public API reference to the docs.
  - See `docs/reference/http-api.md`.
- [x] Add an auto-generated SDK/React reference to the docs with source docstrings.
  - See `typedoc.json`, `npm run docs:reference`, and `docs/reference/sdk/`.
- [x] Add inline links to SDK and API references where appropriate.
  - See `docs/sending-events.md`, `docs/identifying-users.md`, and `docs/reference/index.md`.
- [x] Add a GitHub Action that publishes the ingest Docker image to GitHub Container Registry on pushes to `main`.
  - See `.github/workflows/container.yml`; publishing is gated on successful `CI` completion for `main`.
- [x] Add a Railway deployment docs page.
  - See `docs/railway.md`.
- [x] Review documentation design and keep docs lean and maintainable.
  - Docs are grouped into Start, Operate, and Reference paths from `docs/index.md`.
- [x] Add a Helm chart deployment option with autoscaling behavior and docs.
  - See `deploy/helm/clickhouse-product-analytics/` and `docs/helm.md`.
- [x] Compare the SDK implementation with mature browser analytics SDK patterns and identify stability/performance shortcomings.
  - See `docs/sdk-stability.md`. Key fixes from this pass are implemented in the SDK.
- [x] Add a coding-agent skill for adding product analytics tracking.
  - See `skills/product-analytics-tracking/SKILL.md` and `docs/agent-skill.md`.
- [x] Make SDK and React npm publishing easier with Release It and dry-run tooling.
  - See `.release-it.json`, `scripts/sync-release-version.mjs`, `scripts/release-dry-run.mjs`, and `docs/publishing.md`.
- [x] Make sure license, attribution, README, and third-party notices are correct for packages.
  - See root notices plus package-local `LICENSE`, `README.md`, and `THIRD_PARTY_NOTICES.md` files for SDK and React.
- [x] Review the overall repo again and make it publishable.
  - Added CI/E2E, Pages, CI-gated GHCR publishing, Helm lint/render checks, release dry-runs, package metadata, contributor/security docs, `.dockerignore`, example READMEs, production-safe migration commands, and final verification.
