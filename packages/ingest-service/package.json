{
  "name": "@clickhouse-product-analytics/ingest-service",
  "version": "0.1.0",
  "description": "HTTP ingest service for ClickHouse Product Analytics",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "migrate": "node dist/migrate.js",
    "migrate:dev": "tsx src/migrate.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@clickhouse/client": "^1.12.1",
    "@fastify/cors": "^11.0.1",
    "fastify": "^5.3.2",
    "zod": "^3.24.3"
  }
}
